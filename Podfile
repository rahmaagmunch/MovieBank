# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MovieBank' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MovieBank
  pod 'SVProgressHUD'
  pod 'RxSwift', '6.5.0'
  pod 'RxCocoa', '6.5.0'
  pod 'Kingfisher', '~> 7.0'
  pod 'youtube-ios-player-helper', '~> 1.0.4'

  target 'MovieBankTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MovieBankUITests' do
    # Pods for testing
  end

end
